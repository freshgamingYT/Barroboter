from flask import Blueprint, request, jsonify
import logging

logger = logging.getLogger('my_logger')

set_low_route = Blueprint('set_low_route', __name__)

@set_low_route.route('/set-low', methods=['POST'])
def set_low():
    data = request.get_json()
    logger.debug(f'Received set_low command: {data}')
    # Add your set_low logic here
    return jsonify({'status': 'success'})
